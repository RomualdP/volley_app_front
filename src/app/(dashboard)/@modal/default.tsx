/**
 * Default Modal Slot
 *
 * Retourne null par défaut (pas de modal affiché)
 * Utilisé par les Parallel Routes quand aucune route interceptée n'est active
 */
export default function DefaultModal() {
  return null;
}
